/*
var wnd = $(window);

var scrollElem = $('.scrolling');
scrollElem.on('click', function(e) {
    var self = $(this);
    var target = self.attr('href');
    var position = $(target).offset().top;
    $('html, body').stop().animate({ scrollTop: position }, 400);
    return false;
});*/
